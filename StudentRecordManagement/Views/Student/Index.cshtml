@model List<Student>

<div>
    <div>
        <button><a class="nav-link" asp-controller="Student" asp-action="Create">Create New</a></button>
    </div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Middle Name</th>
                <th>Last Name</th>
                <th>Phone</th>
                <th>Address</th>
                <th>Class</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var student in Model)
            {
                <tr>
                    <td><a asp-controller="Student" asp-action="Create" asp-route-id="@student.StudentId">@student.FirstName</a></td>
                    <td>@student.MiddleName</td>
                    <td>@student.LastName</td>
                    <td>@student.Phone</td>
                    <td>@student.Address</td>
                    <td>@student.ClassName</td>
                </tr>
            }
        </tbody>
    </table>
</div>